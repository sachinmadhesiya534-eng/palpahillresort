import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "Palpa Hill Resort",
  description:
    "Experience peaceful luxury surrounded by nature at Palpa Hill Resort.",
  keywords: ["Palpa", "Resort", "Nepal", "Mud House", "Luxury Resort", "Hotel"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
